# MAD-1-project
Placement-portal
# Initialize git
git init

# Tell git to track all files (except the ones in .gitignore)
git add .

# Commit your changes
git commit -m "Initial commit - Placement Portal project"

# Rename main branch to 'main'
git branch -M main

# Link your local folder to the GitHub repo you just created
# Replace 'YOUR_USERNAME' with your actual GitHub username
git remote add origin https://github.com/24f2004727/PlacementPortal.git

# Push the code to GitHub
git push -u origin main
